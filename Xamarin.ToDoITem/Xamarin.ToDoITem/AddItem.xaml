<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Xamarin.ToDoITem.AddItem">

    <Grid Padding="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <StackLayout Grid.Row="0" Orientation="Horizontal">
            <Label Text="Insert a new task:" FontSize="Large" VerticalOptions="Center"/>
            <Entry x:Name="TaskEntry" Placeholder="Task" FontSize="Large" HorizontalOptions="FillAndExpand" VerticalOptions="Center"/>
        </StackLayout>

        <Button Grid.Row="1" Text="Add" Clicked="AddButton_OnClicked"/>

        <StackLayout Grid.Row="2" BackgroundColor="#2196F3" HeightRequest="2"/>

        <ListView Grid.Row="3" x:Name="ItemsListView" ItemTapped="ItemsListView_OnItemTapped">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <Label Text="{Binding Text}" FontSize="Large"/>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</ContentPage>